# $HOME/.bash_profile
# Personal profile for bash login shells.
# Make sure to source ${HOME}/.bashrc and then ignore this file.

if [ -f "${HOME}/.bashrc" ]
then
    source "${HOME}/.bashrc"
fi

# Local Variables:
# mode:shell-script
# End:
